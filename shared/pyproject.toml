[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "temporalrca-contracts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.8,<3"]

[tool.hatch.build.targets.wheel]
packages = ["temporalrca_contracts"]
