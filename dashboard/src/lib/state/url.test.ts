import { describe,expect,it } from 'vitest';import { LIVE_WINDOW_MS,parseDashboardState,rangeForPreset,stateToSearch } from './url';
describe('dashboard URL state',()=>{it('defaults to a live 24 hour range',()=>{const now=new Date('2026-01-01T12:00:00Z');const state=parseDashboardState(new URL('http://local/'),now);expect(state.live).toBe(true);expect(state.end.getTime()-state.start.getTime()).toBe(LIVE_WINDOW_MS)});it('keeps preset ranges live and rolling',()=>{const first=parseDashboardState(new URL('http://local/?range=15'),new Date('2026-01-01T12:00:00Z'));const next=parseDashboardState(new URL('http://local/?range=15'),new Date('2026-01-01T12:01:00Z'));expect(first.live).toBe(true);expect(first.end.getTime()-first.start.getTime()).toBe(15*60_000);expect(next.start.getTime()-first.start.getTime()).toBe(60_000)});it('round trips paused state and filters',()=>{const original={start:new Date('2026-01-01T10:00:00Z'),end:new Date('2026-01-01T11:00:00Z'),live:false,timezone:'UTC',filters:'severity:error',streams:['cpu','logs'],selected:'host-1'};const state=parseDashboardState(new URL(`http://local/?${stateToSearch(original)}`));expect(state).toEqual(original)});it('calculates deterministic presets',()=>{const now=new Date('2026-01-01T12:00:00Z');expect(rangeForPreset(60,now).start.toISOString()).toBe('2026-01-01T11:00:00.000Z')})});
