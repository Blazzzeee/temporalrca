/**
 * Generated API contract output.
 *
 * Run `docker compose run --rm dashboard-codegen` after the FastAPI service is
 * ready. openapi-typescript replaces this bootstrap declaration with the full
 * schema from `/openapi.json`; UI-specific normalized models live in api.ts.
 */
export interface paths {}
